<%= render partial: 'galleries/nav' %>

<h3><%= @user.username %></h3>

<%= @user.email %>

<%= link_to "Edit", edit_user_path(@user) if current_user == @user %>
<%= link_to "Remove", user_path(@user), method: "DELETE" if gallery_admin? %>